
* service creation should ask - create google drive

* When google auth is created for a user, firebase function should trigger subscription to a calendar
    -- need to write the function that consumes this subscription

* When a user accepts a booking, firebase function should trigger which:
    * adds calendar event
    * includes meet link
    * invites customer
    * adds a shared google drive if that option is set


* create a react app that defines the booking dialog
    * when customer requests booking, firebase function wil send out email to user, link to dtail page w/ confirmation page

* booking representation
    * customer id, user id, timestamp, service, 